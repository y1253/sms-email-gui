// The one published support address. Kept here so the Contact page, the legal
// pages and anything added later can't drift apart. The server has its own copy
// in the HELP SMS (webhooks.service.ts) — keep the two in sync.
export const SUPPORT_EMAIL = 'support@emailontext.com';
